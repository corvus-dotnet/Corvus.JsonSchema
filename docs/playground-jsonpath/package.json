{
  "private": true,
  "name": "corvus-jsonpath-playground-assets",
  "description": "Build assets for the JSONPath Playground (monaco-jsonpath bundle)",
  "scripts": {
    "bundle": "node esbuild.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
