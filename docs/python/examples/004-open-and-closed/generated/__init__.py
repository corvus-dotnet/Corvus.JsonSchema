# AUTO-GENERATED by Corvus.Text.Json (Python engine, single-file). Do not edit.
from __future__ import annotations
from collections.abc import Mapping, Sequence
from typing import Any, Required, TypedDict, cast
from corvus_json_runtime import Ev, NOEV, Results, RmwTarget, apply_merge_patch, build, canonicalize, create_merge_patch, decode_and_parse, fresh, is_num, is_obj, rmw_produce_full, rmw_upsert


class StrictPoint(TypedDict, total=False):
    x: Required[float]
    y: Required[float]


def _eval_strict_point(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_obj(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/004-open-and-closed/point.json#/type")
        ok = False
    if is_obj(value):
        if "x" not in value:
            if r is None:
                return False
            r.fail("/required", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/004-open-and-closed/point.json#/required")
            ok = False
        if "y" not in value:
            if r is None:
                return False
            r.fail("/required", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/004-open-and-closed/point.json#/required")
            ok = False
    if is_obj(value):
        _track = not ev.n
        _i = -1
        for _k in value:
            _i += 1
            if _k == "x":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/x")
                _v = value[_k]
                if not (is_num(_v)):
                    if r is None:
                        return False
                    r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/004-open-and-closed/point.json#/properties/x/type")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
            elif _k == "y":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/y")
                _v = value[_k]
                if not (is_num(_v)):
                    if r is None:
                        return False
                    r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/004-open-and-closed/point.json#/properties/y/type")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
    if is_obj(value):
        _i = -1
        for _k in value:
            _i += 1
            if not ev.has_prop(_i):
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/unevaluatedProperties")
                _c = _eval_json_not_any(value[_k], NOEV, r)
                if r is not None: r.leave()
                if not _c:
                    if r is None:
                        return False
                    ok = False
            ev.mark_prop(_i)
    if r is not None and r.verbose and ok:
        r.annotate("title", "StrictPoint", "/title", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/004-open-and-closed/point.json#/title")
    return ok



def build_strict_point(value: StrictPoint) -> bytes:
    return build(value)


def build_canonical_strict_point(value: StrictPoint) -> bytes:
    return canonicalize(value)


def parse_strict_point(data: bytes | str) -> StrictPoint:
    return cast("StrictPoint", decode_and_parse(data))


def apply_merge_patch_strict_point(doc: StrictPoint | bytes, merge_patch: object) -> bytes:
    return canonicalize(apply_merge_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, merge_patch))


def create_merge_patch_strict_point(source: StrictPoint | bytes, target: StrictPoint | bytes) -> object:
    return create_merge_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def patch_strict_point(source: bytes, changes: Mapping[str, object], removals: Sequence[str] | None = None) -> bytes:
    targets: list[RmwTarget] = []
    if "x" in changes:
        targets.append(RmwTarget("x".encode(), build(changes["x"])))
    if "y" in changes:
        targets.append(RmwTarget("y".encode(), build(changes["y"])))
    if removals:
        return rmw_produce_full(source, targets, [n.encode() for n in removals], [])
    return rmw_upsert(source, targets)



type X = float


def _eval_x(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_num(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/004-open-and-closed/point.json#/properties/x/type")
        ok = False
    return ok



type Y = float


def _eval_y(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_num(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/004-open-and-closed/point.json#/properties/y/type")
        ok = False
    return ok



type JsonNotAny = Any


def _eval_json_not_any(value: object, ev: Ev, r: Results | None) -> bool:
    if r is not None:
        r.fail("", "corvus:/JsonNotAny#")
    return False



def evaluate(value: object, results: Results | None = None) -> bool:
    if isinstance(value, bytes):
        value = decode_and_parse(value)
    return _eval_strict_point(value, fresh(), results)


def parse(data: bytes | str) -> StrictPoint:
    return cast("StrictPoint", decode_and_parse(data))