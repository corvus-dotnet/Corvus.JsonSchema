# AUTO-GENERATED by Corvus.Text.Json (Python engine, single-file). Do not edit.
from __future__ import annotations
from collections.abc import Callable, Mapping, Sequence
from typing import Required, TypedDict, cast
from corvus_json_runtime import Ev, NOEV, Results, RmwArrayEdit, RmwArrayOps, RmwTarget, apply_merge_patch, apply_patch, build, canonicalize, create_merge_patch, create_patch, decode_and_parse, is_arr, is_int, is_num, is_obj, produce, rmw_produce_full, rmw_upsert


class Cart(TypedDict, total=False):
    items: Required[Items]


def _eval_cart(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_obj(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/007-arrays/cart.json#/type")
        ok = False
    if is_obj(value):
        if "items" not in value:
            if r is None:
                return False
            r.fail("/required", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/007-arrays/cart.json#/required")
            ok = False
    if is_obj(value):
        _track = not ev.n
        _i = -1
        for _k in value:
            _i += 1
            if _k == "items":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/items")
                _c = _eval_items(value[_k], NOEV, r)
                if r is not None: r.leave()
                if not _c:
                    if r is None:
                        return False
                    ok = False
                if _track: ev.mark_prop(_i)
    if r is not None and r.verbose and ok:
        r.annotate("title", "Cart", "/title", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/007-arrays/cart.json#/title")
    return ok



def build_cart(value: Cart) -> bytes:
    return build(value)


def build_canonical_cart(value: Cart) -> bytes:
    return canonicalize(value)


def parse_cart(data: bytes | str) -> Cart:
    return cast("Cart", decode_and_parse(data))


def apply_merge_patch_cart(doc: Cart | bytes, merge_patch: object) -> bytes:
    return canonicalize(apply_merge_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, merge_patch))


def create_merge_patch_cart(source: Cart | bytes, target: Cart | bytes) -> object:
    return create_merge_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def apply_patch_cart(doc: Cart | bytes, patch: object) -> bytes:
    return canonicalize(apply_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, patch))


def create_patch_cart(source: Cart | bytes, target: Cart | bytes) -> object:
    return create_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def produce_cart(source: bytes, recipe: Callable[[Cart], None]) -> bytes:
    return produce(source, cast("Callable[[object], None]", recipe))


def patch_cart(source: bytes, changes: Mapping[str, object], removals: Sequence[str] | None = None, arrays: Mapping[str, RmwArrayOps] | None = None) -> bytes:
    targets: list[RmwTarget] = []
    if "items" in changes:
        targets.append(RmwTarget("items".encode(), build(changes["items"])))
    array_edits: list[RmwArrayEdit] = []
    if arrays is not None:
        if "items" in arrays:
            array_edits.append(RmwArrayEdit("items".encode(), arrays["items"], 0))
    if removals or array_edits:
        return rmw_produce_full(source, targets, [n.encode() for n in removals] if removals else [], array_edits)
    return rmw_upsert(source, targets)



type Items = Sequence[LineItem]


def _eval_items(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_arr(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/007-arrays/cart.json#/properties/items/type")
        ok = False
    if is_arr(value) and len(value) < 1:
        if r is None:
            return False
        r.fail("/minItems", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/007-arrays/cart.json#/properties/items/minItems")
        ok = False
    if is_arr(value):
        _track = not ev.n
        for _idx in range(0, len(value)):
            if r is not None: r.enter("/" + str(_idx), "/items")
            _c = _eval_line_item(value[_idx], NOEV, r)
            if r is not None: r.leave()
            if not _c:
                if r is None:
                    return False
                ok = False
            if _track: ev.mark_item(_idx)
    return ok



class LineItem(TypedDict, total=False):
    qty: Required[int]
    sku: Required[str]


def _eval_line_item(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_obj(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/007-arrays/cart.json#/properties/items/items/type")
        ok = False
    if is_obj(value):
        if "sku" not in value:
            if r is None:
                return False
            r.fail("/required", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/007-arrays/cart.json#/properties/items/items/required")
            ok = False
        if "qty" not in value:
            if r is None:
                return False
            r.fail("/required", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/007-arrays/cart.json#/properties/items/items/required")
            ok = False
    if is_obj(value):
        _track = not ev.n
        _i = -1
        for _k in value:
            _i += 1
            if _k == "qty":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/qty")
                _v = value[_k]
                if not (is_int(_v)):
                    if r is None:
                        return False
                    r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/007-arrays/cart.json#/properties/items/items/properties/qty/type")
                    ok = False
                if is_num(_v) and _v < 1:
                    if r is None:
                        return False
                    r.fail("/minimum", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/007-arrays/cart.json#/properties/items/items/properties/qty/minimum")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
            elif _k == "sku":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/sku")
                _v = value[_k]
                if not (isinstance(_v, str)):
                    if r is None:
                        return False
                    r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/007-arrays/cart.json#/properties/items/items/properties/sku/type")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
    if r is not None and r.verbose and ok:
        r.annotate("title", "LineItem", "/title", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/007-arrays/cart.json#/properties/items/items/title")
    return ok



def build_line_item(value: LineItem) -> bytes:
    return build(value)


def build_canonical_line_item(value: LineItem) -> bytes:
    return canonicalize(value)


def parse_line_item(data: bytes | str) -> LineItem:
    return cast("LineItem", decode_and_parse(data))


def apply_merge_patch_line_item(doc: LineItem | bytes, merge_patch: object) -> bytes:
    return canonicalize(apply_merge_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, merge_patch))


def create_merge_patch_line_item(source: LineItem | bytes, target: LineItem | bytes) -> object:
    return create_merge_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def apply_patch_line_item(doc: LineItem | bytes, patch: object) -> bytes:
    return canonicalize(apply_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, patch))


def create_patch_line_item(source: LineItem | bytes, target: LineItem | bytes) -> object:
    return create_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def produce_line_item(source: bytes, recipe: Callable[[LineItem], None]) -> bytes:
    return produce(source, cast("Callable[[object], None]", recipe))


def patch_line_item(source: bytes, changes: Mapping[str, object], removals: Sequence[str] | None = None) -> bytes:
    targets: list[RmwTarget] = []
    if "qty" in changes:
        targets.append(RmwTarget("qty".encode(), build(changes["qty"])))
    if "sku" in changes:
        targets.append(RmwTarget("sku".encode(), build(changes["sku"])))
    if removals:
        return rmw_produce_full(source, targets, [n.encode() for n in removals], [])
    return rmw_upsert(source, targets)



type Qty = int


def _eval_qty(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_int(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/007-arrays/cart.json#/properties/items/items/properties/qty/type")
        ok = False
    if is_num(value) and value < 1:
        if r is None:
            return False
        r.fail("/minimum", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/007-arrays/cart.json#/properties/items/items/properties/qty/minimum")
        ok = False
    return ok



type Sku = str


def _eval_sku(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (isinstance(value, str)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/007-arrays/cart.json#/properties/items/items/properties/sku/type")
        ok = False
    return ok



def evaluate(value: object, results: Results | None = None) -> bool:
    if isinstance(value, bytes):
        value = decode_and_parse(value)
    return _eval_cart(value, NOEV, results)


def parse(data: bytes | str) -> Cart:
    return cast("Cart", decode_and_parse(data))