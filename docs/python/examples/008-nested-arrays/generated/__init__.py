# AUTO-GENERATED by Corvus.Text.Json (Python engine, single-file). Do not edit.
from __future__ import annotations
from collections.abc import Mapping, Sequence
from typing import Required, TypedDict, cast
from corvus_json_runtime import Ev, NOEV, Results, RmwArrayEdit, RmwArrayOps, RmwTarget, apply_merge_patch, build, canonicalize, create_merge_patch, decode_and_parse, is_arr, is_num, is_obj, rmw_produce_full, rmw_upsert


class Grid(TypedDict, total=False):
    cells: Required[Cells]


def _eval_grid(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_obj(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/008-nested-arrays/grid.json#/type")
        ok = False
    if is_obj(value):
        if "cells" not in value:
            if r is None:
                return False
            r.fail("/required", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/008-nested-arrays/grid.json#/required")
            ok = False
    if is_obj(value):
        _track = not ev.n
        _i = -1
        for _k in value:
            _i += 1
            if _k == "cells":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/cells")
                _c = _eval_cells(value[_k], NOEV, r)
                if r is not None: r.leave()
                if not _c:
                    if r is None:
                        return False
                    ok = False
                if _track: ev.mark_prop(_i)
    if r is not None and r.verbose and ok:
        r.annotate("title", "Grid", "/title", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/008-nested-arrays/grid.json#/title")
    return ok



def build_grid(value: Grid) -> bytes:
    return build(value)


def build_canonical_grid(value: Grid) -> bytes:
    return canonicalize(value)


def parse_grid(data: bytes | str) -> Grid:
    return cast("Grid", decode_and_parse(data))


def apply_merge_patch_grid(doc: Grid | bytes, merge_patch: object) -> bytes:
    return canonicalize(apply_merge_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, merge_patch))


def create_merge_patch_grid(source: Grid | bytes, target: Grid | bytes) -> object:
    return create_merge_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def patch_grid(source: bytes, changes: Mapping[str, object], removals: Sequence[str] | None = None, arrays: Mapping[str, RmwArrayOps] | None = None) -> bytes:
    targets: list[RmwTarget] = []
    if "cells" in changes:
        targets.append(RmwTarget("cells".encode(), build(changes["cells"])))
    array_edits: list[RmwArrayEdit] = []
    if arrays is not None:
        if "cells" in arrays:
            array_edits.append(RmwArrayEdit("cells".encode(), arrays["cells"], 0))
    if removals or array_edits:
        return rmw_produce_full(source, targets, [n.encode() for n in removals] if removals else [], array_edits)
    return rmw_upsert(source, targets)



type Cells = Sequence[Items]


def _eval_cells(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_arr(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/008-nested-arrays/grid.json#/properties/cells/type")
        ok = False
    if is_arr(value):
        _track = not ev.n
        for _idx in range(0, len(value)):
            if r is not None: r.enter("/" + str(_idx), "/items")
            _c = _eval_items(value[_idx], NOEV, r)
            if r is not None: r.leave()
            if not _c:
                if r is None:
                    return False
                ok = False
            if _track: ev.mark_item(_idx)
    return ok



type Items = Sequence[float]


def _eval_items(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_arr(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/008-nested-arrays/grid.json#/properties/cells/items/type")
        ok = False
    if is_arr(value):
        _track = not ev.n
        for _idx in range(0, len(value)):
            if r is not None: r.enter("/" + str(_idx), "/items")
            _v = value[_idx]
            if not (is_num(_v)):
                if r is None:
                    return False
                r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/008-nested-arrays/grid.json#/properties/cells/items/items/type")
                ok = False
            if r is not None: r.leave()
            if _track: ev.mark_item(_idx)
    return ok



type Items2 = float


def _eval_items2(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_num(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/008-nested-arrays/grid.json#/properties/cells/items/items/type")
        ok = False
    return ok



def evaluate(value: object, results: Results | None = None) -> bool:
    if isinstance(value, bytes):
        value = decode_and_parse(value)
    return _eval_grid(value, NOEV, results)


def parse(data: bytes | str) -> Grid:
    return cast("Grid", decode_and_parse(data))