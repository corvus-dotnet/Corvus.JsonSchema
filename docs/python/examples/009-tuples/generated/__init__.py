# AUTO-GENERATED by Corvus.Text.Json (Python engine, single-file). Do not edit.
from __future__ import annotations
from collections.abc import Callable, Mapping, Sequence
from typing import Any, Required, TypedDict, cast
from corvus_json_runtime import Ev, NOEV, Results, RmwArrayEdit, RmwArrayOps, RmwTarget, apply_merge_patch, apply_patch, build, canonicalize, create_merge_patch, create_patch, decode_and_parse, is_arr, is_num, is_obj, produce, rmw_produce_full, rmw_upsert


class Point3D(TypedDict, total=False):
    coord: Required[Coord]


def _eval_point3_d(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_obj(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/009-tuples/point3d.json#/type")
        ok = False
    if is_obj(value):
        if "coord" not in value:
            if r is None:
                return False
            r.fail("/required", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/009-tuples/point3d.json#/required")
            ok = False
    if is_obj(value):
        _track = not ev.n
        _i = -1
        for _k in value:
            _i += 1
            if _k == "coord":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/coord")
                _c = _eval_coord(value[_k], NOEV, r)
                if r is not None: r.leave()
                if not _c:
                    if r is None:
                        return False
                    ok = False
                if _track: ev.mark_prop(_i)
    if r is not None and r.verbose and ok:
        r.annotate("title", "Point3D", "/title", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/009-tuples/point3d.json#/title")
    return ok



def build_point3_d(value: Point3D) -> bytes:
    return build(value)


def build_canonical_point3_d(value: Point3D) -> bytes:
    return canonicalize(value)


def parse_point3_d(data: bytes | str) -> Point3D:
    return cast("Point3D", decode_and_parse(data))


def apply_merge_patch_point3_d(doc: Point3D | bytes, merge_patch: object) -> bytes:
    return canonicalize(apply_merge_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, merge_patch))


def create_merge_patch_point3_d(source: Point3D | bytes, target: Point3D | bytes) -> object:
    return create_merge_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def apply_patch_point3_d(doc: Point3D | bytes, patch: object) -> bytes:
    return canonicalize(apply_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, patch))


def create_patch_point3_d(source: Point3D | bytes, target: Point3D | bytes) -> object:
    return create_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def produce_point3_d(source: bytes, recipe: Callable[[Point3D], None]) -> bytes:
    return produce(source, cast("Callable[[object], None]", recipe))


def patch_point3_d(source: bytes, changes: Mapping[str, object], removals: Sequence[str] | None = None, arrays: Mapping[str, RmwArrayOps] | None = None) -> bytes:
    targets: list[RmwTarget] = []
    if "coord" in changes:
        targets.append(RmwTarget("coord".encode(), build(changes["coord"])))
    array_edits: list[RmwArrayEdit] = []
    if arrays is not None:
        if "coord" in arrays:
            array_edits.append(RmwArrayEdit("coord".encode(), arrays["coord"], 0))
    if removals or array_edits:
        return rmw_produce_full(source, targets, [n.encode() for n in removals] if removals else [], array_edits)
    return rmw_upsert(source, targets)



type Coord = tuple[float, float, float]


def _eval_coord(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_arr(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/009-tuples/point3d.json#/properties/coord/type")
        ok = False
    if is_arr(value) and len(value) < 3:
        if r is None:
            return False
        r.fail("/minItems", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/009-tuples/point3d.json#/properties/coord/minItems")
        ok = False
    if is_arr(value):
        _track = not ev.n
        if len(value) > 0:
            if r is not None: r.enter("/0", "/prefixItems/0")
            _c = _eval_prefix_items(value[0], NOEV, r)
            if r is not None: r.leave()
            if not _c:
                if r is None:
                    return False
                ok = False
            if _track: ev.mark_item(0)
        if len(value) > 1:
            if r is not None: r.enter("/1", "/prefixItems/1")
            _c = _eval_prefix_items2(value[1], NOEV, r)
            if r is not None: r.leave()
            if not _c:
                if r is None:
                    return False
                ok = False
            if _track: ev.mark_item(1)
        if len(value) > 2:
            if r is not None: r.enter("/2", "/prefixItems/2")
            _c = _eval_prefix_items3(value[2], NOEV, r)
            if r is not None: r.leave()
            if not _c:
                if r is None:
                    return False
                ok = False
            if _track: ev.mark_item(2)
    if is_arr(value):
        _track = not ev.n
        for _idx in range(3, len(value)):
            if r is not None: r.enter("/" + str(_idx), "/items")
            _c = _eval_json_not_any(value[_idx], NOEV, r)
            if r is not None: r.leave()
            if not _c:
                if r is None:
                    return False
                ok = False
            if _track: ev.mark_item(_idx)
    return ok



type PrefixItems = float


def _eval_prefix_items(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_num(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/009-tuples/point3d.json#/properties/coord/prefixItems/0/type")
        ok = False
    return ok



type PrefixItems2 = float


def _eval_prefix_items2(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_num(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/009-tuples/point3d.json#/properties/coord/prefixItems/1/type")
        ok = False
    return ok



type PrefixItems3 = float


def _eval_prefix_items3(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_num(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/009-tuples/point3d.json#/properties/coord/prefixItems/2/type")
        ok = False
    return ok



type JsonNotAny = Any


def _eval_json_not_any(value: object, ev: Ev, r: Results | None) -> bool:
    if r is not None:
        r.fail("", "corvus:/JsonNotAny#")
    return False



def evaluate(value: object, results: Results | None = None) -> bool:
    if isinstance(value, bytes):
        value = decode_and_parse(value)
    return _eval_point3_d(value, NOEV, results)


def parse(data: bytes | str) -> Point3D:
    return cast("Point3D", decode_and_parse(data))