# AUTO-GENERATED by Corvus.Text.Json (Python engine, single-file). Do not edit.
from __future__ import annotations
from collections.abc import Callable, Mapping, Sequence
from typing import NewType, Required, TypedDict, cast
from corvus_json_runtime import Ev, FormatError, Instant, NOEV, Results, RmwTarget, apply_merge_patch, apply_patch, build, canonicalize, create_merge_patch, create_patch, decode_and_parse, fmt, fresh, is_obj, produce, rmw_produce_full, rmw_upsert, to_instant


class Widget(TypedDict, total=False):
    createdAt: CreatedAt
    id: str
    name: Required[str]


def _eval_widget(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_obj(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/010-mixins/widget.json#/type")
        ok = False
    _allt = fresh()
    if r is not None: r.enter("", "/allOf/0")
    _c = _eval_named(value, _allt, r)
    if r is not None: r.leave()
    if not _c:
        if r is None:
            return False
        ok = False
    ev.merge_props(_allt)
    ev.merge_items(_allt)
    _allt = fresh()
    if r is not None: r.enter("", "/allOf/1")
    _c = _eval_timestamped(value, _allt, r)
    if r is not None: r.leave()
    if not _c:
        if r is None:
            return False
        ok = False
    ev.merge_props(_allt)
    ev.merge_items(_allt)
    if is_obj(value):
        _track = not ev.n
        _i = -1
        for _k in value:
            _i += 1
            if _k == "id":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/id")
                _v = value[_k]
                if not (isinstance(_v, str)):
                    if r is None:
                        return False
                    r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/010-mixins/widget.json#/properties/id/type")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
    if r is not None and r.verbose and ok:
        r.annotate("title", "Widget", "/title", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/010-mixins/widget.json#/title")
    return ok



def build_widget(value: Widget) -> bytes:
    return build(value)


def build_canonical_widget(value: Widget) -> bytes:
    return canonicalize(value)


def parse_widget(data: bytes | str) -> Widget:
    return cast("Widget", decode_and_parse(data))


def apply_merge_patch_widget(doc: Widget | bytes, merge_patch: object) -> bytes:
    return canonicalize(apply_merge_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, merge_patch))


def create_merge_patch_widget(source: Widget | bytes, target: Widget | bytes) -> object:
    return create_merge_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def apply_patch_widget(doc: Widget | bytes, patch: object) -> bytes:
    return canonicalize(apply_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, patch))


def create_patch_widget(source: Widget | bytes, target: Widget | bytes) -> object:
    return create_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def produce_widget(source: bytes, recipe: Callable[[Widget], None]) -> bytes:
    return produce(source, cast("Callable[[object], None]", recipe))


def patch_widget(source: bytes, changes: Mapping[str, object], removals: Sequence[str] | None = None) -> bytes:
    targets: list[RmwTarget] = []
    if "createdAt" in changes:
        targets.append(RmwTarget("createdAt".encode(), build(changes["createdAt"])))
    if "id" in changes:
        targets.append(RmwTarget("id".encode(), build(changes["id"])))
    if "name" in changes:
        targets.append(RmwTarget("name".encode(), build(changes["name"])))
    if removals:
        return rmw_produce_full(source, targets, [n.encode() for n in removals], [])
    return rmw_upsert(source, targets)



class Named(TypedDict, total=False):
    name: Required[str]


def _eval_named(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_obj(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/010-mixins/widget.json#/$defs/named/type")
        ok = False
    if is_obj(value):
        if "name" not in value:
            if r is None:
                return False
            r.fail("/required", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/010-mixins/widget.json#/$defs/named/required")
            ok = False
    if is_obj(value):
        _track = not ev.n
        _i = -1
        for _k in value:
            _i += 1
            if _k == "name":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/name")
                _v = value[_k]
                if not (isinstance(_v, str)):
                    if r is None:
                        return False
                    r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/010-mixins/widget.json#/$defs/named/properties/name/type")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
    if r is not None and r.verbose and ok:
        r.annotate("title", "Named", "/title", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/010-mixins/widget.json#/$defs/named/title")
    return ok



def build_named(value: Named) -> bytes:
    return build(value)


def build_canonical_named(value: Named) -> bytes:
    return canonicalize(value)


def parse_named(data: bytes | str) -> Named:
    return cast("Named", decode_and_parse(data))


def apply_merge_patch_named(doc: Named | bytes, merge_patch: object) -> bytes:
    return canonicalize(apply_merge_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, merge_patch))


def create_merge_patch_named(source: Named | bytes, target: Named | bytes) -> object:
    return create_merge_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def apply_patch_named(doc: Named | bytes, patch: object) -> bytes:
    return canonicalize(apply_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, patch))


def create_patch_named(source: Named | bytes, target: Named | bytes) -> object:
    return create_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def produce_named(source: bytes, recipe: Callable[[Named], None]) -> bytes:
    return produce(source, cast("Callable[[object], None]", recipe))


def patch_named(source: bytes, changes: Mapping[str, object], removals: Sequence[str] | None = None) -> bytes:
    targets: list[RmwTarget] = []
    if "name" in changes:
        targets.append(RmwTarget("name".encode(), build(changes["name"])))
    if removals:
        return rmw_produce_full(source, targets, [n.encode() for n in removals], [])
    return rmw_upsert(source, targets)



type Name = str


def _eval_name(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (isinstance(value, str)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/010-mixins/widget.json#/$defs/named/properties/name/type")
        ok = False
    return ok



class Timestamped(TypedDict, total=False):
    createdAt: CreatedAt


def _eval_timestamped(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_obj(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/010-mixins/widget.json#/$defs/timestamped/type")
        ok = False
    if is_obj(value):
        _track = not ev.n
        _i = -1
        for _k in value:
            _i += 1
            if _k == "createdAt":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/createdAt")
                _c = _eval_created_at(value[_k], NOEV, r)
                if r is not None: r.leave()
                if not _c:
                    if r is None:
                        return False
                    ok = False
                if _track: ev.mark_prop(_i)
    if r is not None and r.verbose and ok:
        r.annotate("title", "Timestamped", "/title", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/010-mixins/widget.json#/$defs/timestamped/title")
    return ok



def build_timestamped(value: Timestamped) -> bytes:
    return build(value)


def build_canonical_timestamped(value: Timestamped) -> bytes:
    return canonicalize(value)


def parse_timestamped(data: bytes | str) -> Timestamped:
    return cast("Timestamped", decode_and_parse(data))


def apply_merge_patch_timestamped(doc: Timestamped | bytes, merge_patch: object) -> bytes:
    return canonicalize(apply_merge_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, merge_patch))


def create_merge_patch_timestamped(source: Timestamped | bytes, target: Timestamped | bytes) -> object:
    return create_merge_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def apply_patch_timestamped(doc: Timestamped | bytes, patch: object) -> bytes:
    return canonicalize(apply_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, patch))


def create_patch_timestamped(source: Timestamped | bytes, target: Timestamped | bytes) -> object:
    return create_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def produce_timestamped(source: bytes, recipe: Callable[[Timestamped], None]) -> bytes:
    return produce(source, cast("Callable[[object], None]", recipe))


def patch_timestamped(source: bytes, changes: Mapping[str, object], removals: Sequence[str] | None = None) -> bytes:
    targets: list[RmwTarget] = []
    if "createdAt" in changes:
        targets.append(RmwTarget("createdAt".encode(), build(changes["createdAt"])))
    if removals:
        return rmw_produce_full(source, targets, [n.encode() for n in removals], [])
    return rmw_upsert(source, targets)



CreatedAt = NewType("CreatedAt", str)


def from_created_at(value: str) -> CreatedAt:
    if not fmt("date-time", value):
        raise FormatError("date-time")
    return CreatedAt(value)


def to_temporal_created_at(value: CreatedAt) -> Instant:
    return to_instant(value)


def _eval_created_at(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (isinstance(value, str)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/010-mixins/widget.json#/$defs/timestamped/properties/createdAt/type")
        ok = False
    if isinstance(value, str) and not fmt("date-time", value):
        if r is None:
            return False
        r.fail("/format", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/010-mixins/widget.json#/$defs/timestamped/properties/createdAt/format")
        ok = False
    if r is not None and r.verbose and ok:
        r.annotate("format", "date-time", "/format", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/010-mixins/widget.json#/$defs/timestamped/properties/createdAt/format")
    return ok



type Id = str


def _eval_id(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (isinstance(value, str)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/010-mixins/widget.json#/properties/id/type")
        ok = False
    return ok



def evaluate(value: object, results: Results | None = None) -> bool:
    if isinstance(value, bytes):
        value = decode_and_parse(value)
    return _eval_widget(value, NOEV, results)


def parse(data: bytes | str) -> Widget:
    return cast("Widget", decode_and_parse(data))