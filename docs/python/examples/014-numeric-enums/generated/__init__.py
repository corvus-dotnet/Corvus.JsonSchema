# AUTO-GENERATED by Corvus.Text.Json (Python engine, single-file). Do not edit.
from __future__ import annotations
from collections.abc import Callable, Mapping, Sequence
from typing import Literal, Required, TypedDict, cast
from corvus_json_runtime import Ev, NOEV, Results, RmwTarget, apply_merge_patch, apply_patch, build, canonicalize, create_merge_patch, create_patch, decode_and_parse, eq, is_obj, produce, rmw_produce_full, rmw_upsert


class Response(TypedDict, total=False):
    status: Required[Status]


def _eval_response(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_obj(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/014-numeric-enums/response.json#/type")
        ok = False
    if is_obj(value):
        if "status" not in value:
            if r is None:
                return False
            r.fail("/required", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/014-numeric-enums/response.json#/required")
            ok = False
    if is_obj(value):
        _track = not ev.n
        _i = -1
        for _k in value:
            _i += 1
            if _k == "status":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/status")
                _v = value[_k]
                if not any(eq(_v, _a) for _a in [200, 404, 500]):
                    if r is None:
                        return False
                    r.fail("/enum", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/014-numeric-enums/response.json#/properties/status/enum")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
    if r is not None and r.verbose and ok:
        r.annotate("title", "Response", "/title", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/014-numeric-enums/response.json#/title")
    return ok



def build_response(value: Response) -> bytes:
    return build(value)


def build_canonical_response(value: Response) -> bytes:
    return canonicalize(value)


def parse_response(data: bytes | str) -> Response:
    return cast("Response", decode_and_parse(data))


def apply_merge_patch_response(doc: Response | bytes, merge_patch: object) -> bytes:
    return canonicalize(apply_merge_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, merge_patch))


def create_merge_patch_response(source: Response | bytes, target: Response | bytes) -> object:
    return create_merge_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def apply_patch_response(doc: Response | bytes, patch: object) -> bytes:
    return canonicalize(apply_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, patch))


def create_patch_response(source: Response | bytes, target: Response | bytes) -> object:
    return create_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def produce_response(source: bytes, recipe: Callable[[Response], None]) -> bytes:
    return produce(source, cast("Callable[[object], None]", recipe))


def patch_response(source: bytes, changes: Mapping[str, object], removals: Sequence[str] | None = None) -> bytes:
    targets: list[RmwTarget] = []
    if "status" in changes:
        targets.append(RmwTarget("status".encode(), build(changes["status"])))
    if removals:
        return rmw_produce_full(source, targets, [n.encode() for n in removals], [])
    return rmw_upsert(source, targets)



type Status = Literal[200, 404, 500]


def _eval_status(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    _allowed: list[object] = [200, 404, 500]
    if not any(eq(value, _a) for _a in _allowed):
        if r is None:
            return False
        r.fail("/enum", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/014-numeric-enums/response.json#/properties/status/enum")
        ok = False
    return ok



def evaluate(value: object, results: Results | None = None) -> bool:
    if isinstance(value, bytes):
        value = decode_and_parse(value)
    return _eval_response(value, NOEV, results)


def parse(data: bytes | str) -> Response:
    return cast("Response", decode_and_parse(data))