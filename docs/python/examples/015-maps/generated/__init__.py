# AUTO-GENERATED by Corvus.Text.Json (Python engine, single-file). Do not edit.
from __future__ import annotations
from collections.abc import Mapping
from typing import cast
from corvus_json_runtime import Ev, NOEV, Results, apply_merge_patch, build, canonicalize, create_merge_patch, decode_and_parse, fresh, is_num, is_obj


type Scores = Mapping[str, float]


def _eval_scores(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_obj(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/015-maps/scores.json#/type")
        ok = False
    if is_obj(value):
        _track = not ev.n
        _i = -1
        for _k in value:
            _i += 1
            if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/additionalProperties")
            _c = _eval_additional_properties(value[_k], NOEV, r)
            if r is not None: r.leave()
            if not _c:
                if r is None:
                    return False
                ok = False
            if _track: ev.mark_prop(_i)
    if r is not None and r.verbose and ok:
        r.annotate("title", "Scores", "/title", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/015-maps/scores.json#/title")
    return ok



def build_scores(value: Scores) -> bytes:
    return build(value)


def build_canonical_scores(value: Scores) -> bytes:
    return canonicalize(value)


def parse_scores(data: bytes | str) -> Scores:
    return cast("Scores", decode_and_parse(data))


def apply_merge_patch_scores(doc: Scores | bytes, merge_patch: object) -> bytes:
    return canonicalize(apply_merge_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, merge_patch))


def create_merge_patch_scores(source: Scores | bytes, target: Scores | bytes) -> object:
    return create_merge_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )



type AdditionalProperties = float


def _eval_additional_properties(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_num(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/015-maps/scores.json#/additionalProperties/type")
        ok = False
    if is_num(value) and value < 0:
        if r is None:
            return False
        r.fail("/minimum", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/015-maps/scores.json#/additionalProperties/minimum")
        ok = False
    return ok



def evaluate(value: object, results: Results | None = None) -> bool:
    if isinstance(value, bytes):
        value = decode_and_parse(value)
    return _eval_scores(value, fresh(), results)


def parse(data: bytes | str) -> Scores:
    return cast("Scores", decode_and_parse(data))