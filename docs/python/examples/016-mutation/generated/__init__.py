# AUTO-GENERATED by Corvus.Text.Json (Python engine, single-file). Do not edit.
from __future__ import annotations
from collections.abc import Callable, Mapping, Sequence
from typing import Required, TypedDict, cast
from corvus_json_runtime import Ev, NOEV, Results, RmwArrayEdit, RmwArrayOps, RmwTarget, apply_merge_patch, apply_patch, build, canonicalize, create_merge_patch, create_patch, decode_and_parse, is_arr, is_int, is_obj, produce, rmw_produce_full, rmw_upsert


class Document(TypedDict, total=False):
    id: Required[str]
    tags: Tags
    title: str
    version: int


def _eval_document(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_obj(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/016-mutation/document.json#/type")
        ok = False
    if is_obj(value):
        if "id" not in value:
            if r is None:
                return False
            r.fail("/required", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/016-mutation/document.json#/required")
            ok = False
    if is_obj(value):
        _track = not ev.n
        _i = -1
        for _k in value:
            _i += 1
            if _k == "id":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/id")
                _v = value[_k]
                if not (isinstance(_v, str)):
                    if r is None:
                        return False
                    r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/016-mutation/document.json#/properties/id/type")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
            elif _k == "tags":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/tags")
                _c = _eval_tags(value[_k], NOEV, r)
                if r is not None: r.leave()
                if not _c:
                    if r is None:
                        return False
                    ok = False
                if _track: ev.mark_prop(_i)
            elif _k == "title":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/title")
                _v = value[_k]
                if not (isinstance(_v, str)):
                    if r is None:
                        return False
                    r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/016-mutation/document.json#/properties/title/type")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
            elif _k == "version":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/version")
                _v = value[_k]
                if not (is_int(_v)):
                    if r is None:
                        return False
                    r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/016-mutation/document.json#/properties/version/type")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
    if r is not None and r.verbose and ok:
        r.annotate("title", "Document", "/title", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/016-mutation/document.json#/title")
    return ok



def build_document(value: Document) -> bytes:
    return build(value)


def build_canonical_document(value: Document) -> bytes:
    return canonicalize(value)


def parse_document(data: bytes | str) -> Document:
    return cast("Document", decode_and_parse(data))


def apply_merge_patch_document(doc: Document | bytes, merge_patch: object) -> bytes:
    return canonicalize(apply_merge_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, merge_patch))


def create_merge_patch_document(source: Document | bytes, target: Document | bytes) -> object:
    return create_merge_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def apply_patch_document(doc: Document | bytes, patch: object) -> bytes:
    return canonicalize(apply_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, patch))


def create_patch_document(source: Document | bytes, target: Document | bytes) -> object:
    return create_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def produce_document(source: bytes, recipe: Callable[[Document], None]) -> bytes:
    return produce(source, cast("Callable[[object], None]", recipe))


def patch_document(source: bytes, changes: Mapping[str, object], removals: Sequence[str] | None = None, arrays: Mapping[str, RmwArrayOps] | None = None) -> bytes:
    targets: list[RmwTarget] = []
    if "id" in changes:
        targets.append(RmwTarget("id".encode(), build(changes["id"])))
    if "tags" in changes:
        targets.append(RmwTarget("tags".encode(), build(changes["tags"])))
    if "title" in changes:
        targets.append(RmwTarget("title".encode(), build(changes["title"])))
    if "version" in changes:
        targets.append(RmwTarget("version".encode(), build(changes["version"])))
    array_edits: list[RmwArrayEdit] = []
    if arrays is not None:
        if "tags" in arrays:
            array_edits.append(RmwArrayEdit("tags".encode(), arrays["tags"], 0))
    if removals or array_edits:
        return rmw_produce_full(source, targets, [n.encode() for n in removals] if removals else [], array_edits)
    return rmw_upsert(source, targets)


def with_defaults_document(value: Document) -> Document:
    out: dict[str, object] = dict(value)
    if "title" not in value:
        out["title"] = "Untitled"
    if "version" not in value:
        out["version"] = 1
    return cast("Document", out)



type Id = str


def _eval_id(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (isinstance(value, str)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/016-mutation/document.json#/properties/id/type")
        ok = False
    return ok



type Tags = Sequence[str]


def _eval_tags(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_arr(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/016-mutation/document.json#/properties/tags/type")
        ok = False
    if is_arr(value):
        _track = not ev.n
        for _idx in range(0, len(value)):
            if r is not None: r.enter("/" + str(_idx), "/items")
            _v = value[_idx]
            if not (isinstance(_v, str)):
                if r is None:
                    return False
                r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/016-mutation/document.json#/properties/tags/items/type")
                ok = False
            if r is not None: r.leave()
            if _track: ev.mark_item(_idx)
    return ok



type Items = str


def _eval_items(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (isinstance(value, str)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/016-mutation/document.json#/properties/tags/items/type")
        ok = False
    return ok



type Title = str


def _eval_title(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (isinstance(value, str)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/016-mutation/document.json#/properties/title/type")
        ok = False
    if r is not None and r.verbose and ok:
        r.annotate("default", "Untitled", "/default", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/016-mutation/document.json#/properties/title/default")
    return ok



type Version = int


def _eval_version(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_int(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/016-mutation/document.json#/properties/version/type")
        ok = False
    if r is not None and r.verbose and ok:
        r.annotate("default", 1, "/default", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/016-mutation/document.json#/properties/version/default")
    return ok



def evaluate(value: object, results: Results | None = None) -> bool:
    if isinstance(value, bytes):
        value = decode_and_parse(value)
    return _eval_document(value, NOEV, results)


def parse(data: bytes | str) -> Document:
    return cast("Document", decode_and_parse(data))