# AUTO-GENERATED by Corvus.Text.Json (Python engine, single-file). Do not edit.
from __future__ import annotations
from collections.abc import Callable, Mapping, Sequence
from typing import Any, TypedDict, cast
from corvus_json_runtime import Ev, NOEV, Results, RmwTarget, apply_merge_patch, apply_patch, build, canonicalize, create_merge_patch, create_patch, decode_and_parse, fresh, is_int, is_obj, produce, rmw_produce_full, rmw_upsert


class Settings(TypedDict, total=False):
    fontSize: int
    theme: str


def _eval_settings(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_obj(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/018-defaults/settings.json#/type")
        ok = False
    if is_obj(value):
        _track = not ev.n
        _i = -1
        for _k in value:
            _i += 1
            _m = False
            if _k == "fontSize":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/fontSize")
                _v = value[_k]
                if not (is_int(_v)):
                    if r is None:
                        return False
                    r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/018-defaults/settings.json#/properties/fontSize/type")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
                _m = True
            elif _k == "theme":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/theme")
                _v = value[_k]
                if not (isinstance(_v, str)):
                    if r is None:
                        return False
                    r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/018-defaults/settings.json#/properties/theme/type")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
                _m = True
            if not _m:
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/additionalProperties")
                _c = _eval_json_not_any(value[_k], NOEV, r)
                if r is not None: r.leave()
                if not _c:
                    if r is None:
                        return False
                    ok = False
                if _track: ev.mark_prop(_i)
    if r is not None and r.verbose and ok:
        r.annotate("title", "Settings", "/title", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/018-defaults/settings.json#/title")
    return ok



def build_settings(value: Settings) -> bytes:
    return build(value)


def build_canonical_settings(value: Settings) -> bytes:
    return canonicalize(value)


def parse_settings(data: bytes | str) -> Settings:
    return cast("Settings", decode_and_parse(data))


def apply_merge_patch_settings(doc: Settings | bytes, merge_patch: object) -> bytes:
    return canonicalize(apply_merge_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, merge_patch))


def create_merge_patch_settings(source: Settings | bytes, target: Settings | bytes) -> object:
    return create_merge_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def apply_patch_settings(doc: Settings | bytes, patch: object) -> bytes:
    return canonicalize(apply_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, patch))


def create_patch_settings(source: Settings | bytes, target: Settings | bytes) -> object:
    return create_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def produce_settings(source: bytes, recipe: Callable[[Settings], None]) -> bytes:
    return produce(source, cast("Callable[[object], None]", recipe))


def patch_settings(source: bytes, changes: Mapping[str, object], removals: Sequence[str] | None = None) -> bytes:
    targets: list[RmwTarget] = []
    if "fontSize" in changes:
        targets.append(RmwTarget("fontSize".encode(), build(changes["fontSize"])))
    if "theme" in changes:
        targets.append(RmwTarget("theme".encode(), build(changes["theme"])))
    if removals:
        return rmw_produce_full(source, targets, [n.encode() for n in removals], [])
    return rmw_upsert(source, targets)


def with_defaults_settings(value: Settings) -> Settings:
    out: dict[str, object] = dict(value)
    if "fontSize" not in value:
        out["fontSize"] = 14
    if "theme" not in value:
        out["theme"] = "light"
    return cast("Settings", out)



type FontSize = int


def _eval_font_size(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_int(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/018-defaults/settings.json#/properties/fontSize/type")
        ok = False
    if r is not None and r.verbose and ok:
        r.annotate("default", 14, "/default", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/018-defaults/settings.json#/properties/fontSize/default")
    return ok



type Theme = str


def _eval_theme(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (isinstance(value, str)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/018-defaults/settings.json#/properties/theme/type")
        ok = False
    if r is not None and r.verbose and ok:
        r.annotate("default", "light", "/default", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/018-defaults/settings.json#/properties/theme/default")
    return ok



type JsonNotAny = Any


def _eval_json_not_any(value: object, ev: Ev, r: Results | None) -> bool:
    if r is not None:
        r.fail("", "corvus:/JsonNotAny#")
    return False



def evaluate(value: object, results: Results | None = None) -> bool:
    if isinstance(value, bytes):
        value = decode_and_parse(value)
    return _eval_settings(value, fresh(), results)


def parse(data: bytes | str) -> Settings:
    return cast("Settings", decode_and_parse(data))