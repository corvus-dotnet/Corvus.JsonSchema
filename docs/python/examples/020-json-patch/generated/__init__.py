# AUTO-GENERATED by Corvus.Text.Json (Python engine, single-file). Do not edit.
from __future__ import annotations
from collections.abc import Mapping, Sequence
from typing import Required, TypedDict, cast
from corvus_json_runtime import Ev, NOEV, Results, RmwArrayEdit, RmwArrayOps, RmwTarget, apply_merge_patch, build, canonicalize, create_merge_patch, decode_and_parse, is_arr, is_int, is_obj, rmw_produce_full, rmw_upsert


class Contact(TypedDict, total=False):
    address: Address
    displayName: str
    email: str
    name: Required[str]
    phones: Phones
    version: int


def _eval_contact(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_obj(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/type")
        ok = False
    if is_obj(value):
        if "name" not in value:
            if r is None:
                return False
            r.fail("/required", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/required")
            ok = False
    if is_obj(value):
        _track = not ev.n
        _i = -1
        for _k in value:
            _i += 1
            if _k == "address":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/address")
                _c = _eval_address(value[_k], NOEV, r)
                if r is not None: r.leave()
                if not _c:
                    if r is None:
                        return False
                    ok = False
                if _track: ev.mark_prop(_i)
            elif _k == "displayName":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/displayName")
                _v = value[_k]
                if not (isinstance(_v, str)):
                    if r is None:
                        return False
                    r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/properties/displayName/type")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
            elif _k == "email":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/email")
                _v = value[_k]
                if not (isinstance(_v, str)):
                    if r is None:
                        return False
                    r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/properties/email/type")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
            elif _k == "name":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/name")
                _v = value[_k]
                if not (isinstance(_v, str)):
                    if r is None:
                        return False
                    r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/properties/name/type")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
            elif _k == "phones":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/phones")
                _c = _eval_phones(value[_k], NOEV, r)
                if r is not None: r.leave()
                if not _c:
                    if r is None:
                        return False
                    ok = False
                if _track: ev.mark_prop(_i)
            elif _k == "version":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/version")
                _v = value[_k]
                if not (is_int(_v)):
                    if r is None:
                        return False
                    r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/properties/version/type")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
    if r is not None and r.verbose and ok:
        r.annotate("title", "Contact", "/title", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/title")
    return ok



def build_contact(value: Contact) -> bytes:
    return build(value)


def build_canonical_contact(value: Contact) -> bytes:
    return canonicalize(value)


def parse_contact(data: bytes | str) -> Contact:
    return cast("Contact", decode_and_parse(data))


def apply_merge_patch_contact(doc: Contact | bytes, merge_patch: object) -> bytes:
    return canonicalize(apply_merge_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, merge_patch))


def create_merge_patch_contact(source: Contact | bytes, target: Contact | bytes) -> object:
    return create_merge_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def patch_contact(source: bytes, changes: Mapping[str, object], removals: Sequence[str] | None = None, arrays: Mapping[str, RmwArrayOps] | None = None) -> bytes:
    targets: list[RmwTarget] = []
    if "address" in changes:
        targets.append(RmwTarget("address".encode(), build(changes["address"])))
    if "displayName" in changes:
        targets.append(RmwTarget("displayName".encode(), build(changes["displayName"])))
    if "email" in changes:
        targets.append(RmwTarget("email".encode(), build(changes["email"])))
    if "name" in changes:
        targets.append(RmwTarget("name".encode(), build(changes["name"])))
    if "phones" in changes:
        targets.append(RmwTarget("phones".encode(), build(changes["phones"])))
    if "version" in changes:
        targets.append(RmwTarget("version".encode(), build(changes["version"])))
    array_edits: list[RmwArrayEdit] = []
    if arrays is not None:
        if "phones" in arrays:
            array_edits.append(RmwArrayEdit("phones".encode(), arrays["phones"], 0))
    if removals or array_edits:
        return rmw_produce_full(source, targets, [n.encode() for n in removals] if removals else [], array_edits)
    return rmw_upsert(source, targets)



class Address(TypedDict, total=False):
    city: str
    zip: str


def _eval_address(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_obj(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/properties/address/type")
        ok = False
    if is_obj(value):
        _track = not ev.n
        _i = -1
        for _k in value:
            _i += 1
            if _k == "city":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/city")
                _v = value[_k]
                if not (isinstance(_v, str)):
                    if r is None:
                        return False
                    r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/properties/address/properties/city/type")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
            elif _k == "zip":
                if r is not None: r.enter("/" + _k.replace("~", "~0").replace("/", "~1"), "/properties/zip")
                _v = value[_k]
                if not (isinstance(_v, str)):
                    if r is None:
                        return False
                    r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/properties/address/properties/zip/type")
                    ok = False
                if r is not None: r.leave()
                if _track: ev.mark_prop(_i)
    return ok



def build_address(value: Address) -> bytes:
    return build(value)


def build_canonical_address(value: Address) -> bytes:
    return canonicalize(value)


def parse_address(data: bytes | str) -> Address:
    return cast("Address", decode_and_parse(data))


def apply_merge_patch_address(doc: Address | bytes, merge_patch: object) -> bytes:
    return canonicalize(apply_merge_patch(decode_and_parse(doc) if isinstance(doc, bytes) else doc, merge_patch))


def create_merge_patch_address(source: Address | bytes, target: Address | bytes) -> object:
    return create_merge_patch(
        decode_and_parse(source) if isinstance(source, bytes) else source,
        decode_and_parse(target) if isinstance(target, bytes) else target,
    )


def patch_address(source: bytes, changes: Mapping[str, object], removals: Sequence[str] | None = None) -> bytes:
    targets: list[RmwTarget] = []
    if "city" in changes:
        targets.append(RmwTarget("city".encode(), build(changes["city"])))
    if "zip" in changes:
        targets.append(RmwTarget("zip".encode(), build(changes["zip"])))
    if removals:
        return rmw_produce_full(source, targets, [n.encode() for n in removals], [])
    return rmw_upsert(source, targets)



type City = str


def _eval_city(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (isinstance(value, str)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/properties/address/properties/city/type")
        ok = False
    return ok



type Zip = str


def _eval_zip(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (isinstance(value, str)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/properties/address/properties/zip/type")
        ok = False
    return ok



type DisplayName = str


def _eval_display_name(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (isinstance(value, str)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/properties/displayName/type")
        ok = False
    return ok



type Email = str


def _eval_email(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (isinstance(value, str)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/properties/email/type")
        ok = False
    return ok



type Name = str


def _eval_name(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (isinstance(value, str)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/properties/name/type")
        ok = False
    return ok



type Phones = Sequence[str]


def _eval_phones(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_arr(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/properties/phones/type")
        ok = False
    if is_arr(value):
        _track = not ev.n
        for _idx in range(0, len(value)):
            if r is not None: r.enter("/" + str(_idx), "/items")
            _v = value[_idx]
            if not (isinstance(_v, str)):
                if r is None:
                    return False
                r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/properties/phones/items/type")
                ok = False
            if r is not None: r.leave()
            if _track: ev.mark_item(_idx)
    return ok



type Items = str


def _eval_items(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (isinstance(value, str)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/properties/phones/items/type")
        ok = False
    return ok



type Version = int


def _eval_version(value: object, ev: Ev, r: Results | None) -> bool:
    ok = True
    if not (is_int(value)):
        if r is None:
            return False
        r.fail("/type", "/home/mwa/src/Corvus.JsonSchema/.claude/worktrees/ts-codegen-design/docs/python/examples/020-json-patch/contact.json#/properties/version/type")
        ok = False
    return ok



def evaluate(value: object, results: Results | None = None) -> bool:
    if isinstance(value, bytes):
        value = decode_and_parse(value)
    return _eval_contact(value, NOEV, results)


def parse(data: bytes | str) -> Contact:
    return cast("Contact", decode_and_parse(data))