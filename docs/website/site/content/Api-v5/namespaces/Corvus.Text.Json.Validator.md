Runtime JSON Schema validation without ahead-of-time code generation. Dynamically load, compile, and validate JSON documents against JSON Schema at runtime, supporting all drafts (4, 6, 7, 2019-09, and 2020-12).

Use this namespace when schemas are not known at build time and must be loaded dynamically from files, APIs, or user input.
