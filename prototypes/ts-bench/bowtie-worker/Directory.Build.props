<!-- Empty, on purpose: stops MSBuild walking up to the repo's strict StyleCop /
     TreatWarningsAsErrors props. This is harness tooling, built standalone (not in the solution),
     bundled into the Bowtie OCI image — it is not part of the shipped product. -->
<Project>
</Project>
