# Local, uncommitted GitHub App credentials for the designer's Git integration (§4.7 / D3).
# Copy github-oauth.local.json.example to github-oauth.local.json and fill in your own GitHub OAuth App's
# client id + secret. NEVER commit real secrets — this file keeps them out of the repo.
github-oauth.local.json
