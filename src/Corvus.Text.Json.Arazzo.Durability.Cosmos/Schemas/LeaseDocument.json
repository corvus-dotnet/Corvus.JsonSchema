{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "LeaseDocument",
  "description": "The Azure Cosmos DB document shape for a single-owner workflow lease.",
  "type": "object",
  "required": ["id", "owner", "token", "expiresAt", "epoch"],
  "properties": {
    "id": {
      "type": "string",
      "description": "The run id the lease guards (also the partition key)."
    },
    "owner": {
      "type": "string",
      "description": "The lease owner."
    },
    "token": {
      "type": "string",
      "description": "The lease token, proving ownership for release."
    },
    "expiresAt": {
      "type": "integer",
      "format": "int64",
      "description": "Lease expiry, Unix milliseconds."
    },
    "epoch": {
      "type": "integer",
      "format": "int64",
      "minimum": 1,
      "description": "The grant's epoch: a per-run counter advanced on every grant of this run's lease, persisted so it survives release, expiry and the process that granted it."
    }
  },
  "additionalProperties": true
}
