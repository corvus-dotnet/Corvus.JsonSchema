{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Validation Detail",
  "description": "An array of schema validation results for diagnostic output.",
  "$ref": "#/$defs/ValidationDetailArray",
  "$defs": {
    "ValidationDetailArray": {
      "type": "array",
      "items": { "$ref": "#/$defs/ValidationDetailItem" }
    },
    "ValidationDetailItem": {
      "type": "object",
      "required": ["valid", "evaluationPath", "schemaLocation", "instanceLocation"],
      "properties": {
        "valid": {
          "type": "boolean",
          "description": "Whether the validation passed at this evaluation point."
        },
        "evaluationPath": {
          "type": "string",
          "description": "The evaluation path within the schema."
        },
        "schemaLocation": {
          "type": "string",
          "description": "The location of the schema that was evaluated."
        },
        "instanceLocation": {
          "type": "string",
          "description": "The location within the instance being validated."
        },
        "message": {
          "type": "string",
          "description": "An optional message describing the validation result."
        }
      },
      "additionalProperties": false
    }
  }
}
